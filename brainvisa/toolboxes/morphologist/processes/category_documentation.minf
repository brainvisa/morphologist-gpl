<?xml version="1.0" encoding="iso-8859-1" ?>
<minf version="1.0">

<xhtml name="en">
  <h1>Morphologist</h1>

  <p>Morphologist is the new name for the toolbox including T1 anatomical MRI processing, cortical sulci extraction and identification, and morphometric applications.
  </p>
  <p>
  Before processing T1-weighted data, it is recommended to import it in a BrainVISA database. See the following categories for more information :
  </p>
  <p>
  <a href="bvcategory://data management"><_t_>data management</_t_></a> /
  <a href="bvcategory://data management/import"><_t_>import</_t_></a> /
  <a href="bvprocess://ImportT1MRI"><_t_>T1 MRI</_t_></a>
  </p>
  <p>
  Then, the main pipeline extracts various anatomical objects (hemispheres, grey matter, white matter, cortical sulci...). The first step of this pipeline requires to manually specify to BrainVISA the image orientation (axial, coronal, sagittal...). If you have spatially normalized your data with SPM or MRItotal, you will just have to specify it (be careful if using SPM2 or older, with conventions for left and right...). Otherwise, you may let BrainVisa use SPM or FSL to perform normalization, or you may help brainVISA to virtually normalize your data, clicking on anterior and posterior commissures. All this is done in the process:
     <ul>
    <li><a href="bvprocess://morphologist"><bvProcessName name="Morphologist 2012"/></a></li>
    </ul>
  </p>
  <p>
  Some of the components can be individually triggered in expert mode and may overcome problems occuring with the default pipeline parameters settings.
  The organization of these new components is compatible with the older (2011, 2007, 2004) pipelines.
  This means that they can be used to replace one old component, using the validation flags to control the pipeline behaviour.
  One of the key refinement is a better bias correction that should work better with low grey/white contrast relative to bias magnitude.
  </p>

  <h2>Database organization for T1-weighted data</h2>
  <p>T1-weighted data are stored in a directory <i>database/protocol/subject/</i><b>t1mri</b>. Database, protocol and subject directories names are choosen by the user but t1mri is a fixed name. </p>
  <p><b>t1mri</b> directory contains :
    <ul><li>One or several <i>acquisition</i> directory, named by the user. Each contains :
      <ul><li>Raw data</li>
      <li>One or several <i>analysis</i> directory, named by the user, and which contains the results of an analysis on the raw data, more precisely :
        <ul><li><b>segmentation</b> directory : contains data generated by the segmentation pipeline and a <b>mesh</b> directory containing meshes generated by the pipeline.
          </li>
          <li><b>folds</b> directory : contains <b>3.0</b> and <b>3.1</b> directories for the different versions of  sulci graphs.</li>
          </ul>
        </li></ul>
    </li></ul></p>
  <p>Referentials and transformations for the data are stored in <i>database/protocol/subject/</i><b>registration</b>.</p>

  <h3>Example :</h3>
  <img src="bvimage://database_t1_example.png" />

</xhtml>

<xhtml name="fr">
  <h1>Analyse des images pond�r�es en T1</h1>

  <p>
  Avant de traiter des donn�es d'IRM T1 anatomique, il est recommand� de les importer dans une base de donn�e BrainVISA. Reportez-vous aux cat�gories suivantes pour plus d'information :
  <p>
  <a href="bvcategory://data management"><_t_>data management</_t_></a> /
  <a href="bvcategory://data management/import"><_t_>import</_t_></a> /
  <a href="bvprocess://ImportT1MRI"><_t_>T1 MRI</_t_></a>
  </p>
  </p>
  <p>
  Ensuite, le pipeline principal effectue l'extraction de diff�rents objets anatomiques (h�misph�res, mati�re grise, mati�re blanche, sillons corticaux). La premi�re �tape de ce pipeline demande de sp�cifier manuellement � brainVISA l'orientation de vos acquisitions (axiale, coronale ou sagittale...). Si vous avez normalis� spatialement vos images au pr�alable avec SPM ou MRItotal, il devrait vous suffire de le sp�cifier (m�fiez vous des conventions de SPM2 ou pr�c�dents en ce qui concerne la gauche et la droite). Autrement, vous pouvez laisser BrainVisa utiliser SPM ou FSL pour normaliser les donn�es, ou bien vous devrez cliquer sur quelques points, en particulier les commissures ant�rieure et post�rieure, pour permettre � brainVISA de normaliser virtuellement vos donn�es. Tout cela concerne le traitement:
  </p>
  <ul>
    <li><a href="bvprocess://morphologist"><bvProcessName name="Morphologist 2011"/></a></li>
  </ul>
  <p>
  Certains de ses composants peuvent �tre utilis�s individuellement dans le mode expert et pourraient r�soudre des probl�mes rencontr�s avec les r�glages par d�faut du pipeline Morphologist.
  L'organisation de ces composants est compatible avec les anciens pipelines (2007, 2004).
  Pratiquement, ceci signifie qu'ils peuvent �tre utilis�s pour remplacer un ancien traitement, dans la mesure ou les briques de validation permettent
  d'emp�cher le pipeline g�n�ral d'�craser ces nouveaux r�sultats.
  Une des am�liorations notoire est un nouvel algorithme de correction de biais qui devrait mieux se comporter avec les images dot�es d'un faible contraste gris/blanc relativement � l'amplitude du biais.
  </p>
    <h2>Database organization for T1-weighted data</h2>
  <p>T1-weighted data are stored in a directory <i>database/protocol/subject/</i><b>t1mri</b>. Database, protocol and subject directories names are choosen by the user but t1mri is a fixed name. </p>
  <p><b>t1mri</b> directory contains :
    <ul><li>One or several <i>acquisition</i> directory, named by the user. Each contains :
      <ul><li>Raw data</li>
      <li>One or several <i>analysis</i> directory, named by the user, and which contains the results of an analysis on the raw data, more precisely :
        <ul><li><b>segmentation</b> directory : contains data generated by the segmentation pipeline and a <b>mesh</b> directory containing meshes generated by the pipeline.
          </li>
          <li><b>folds</b> directory : contains <b>3.0</b> and <b>3.1</b> directories for the different versions of  sulci graphs.</li>
          </ul>
        </li></ul>
    </li></ul></p>
  <p>Referentials and transformations for the data are stored in <i>database/protocol/subject/</i><b>registration</b>.</p>

  <h3>Example :</h3>
  <img src="bvimage://database_t1_example.png" />

</xhtml>

</minf>
